from module1 import add, multiplication

if __name__ == "__main__":
    add_val = add(2, 3)
    mult_val = multiplication(2, 3)

    print(f"Two numbers: 2, 3 || add = {add_val}, multiply = {mult_val}")
